#include <bits/stdc++.h>
#define REP(i,n) for(int i = 0; i< (n);i++)
#define all(x) (x).begin(), (x).end()
#define f first  
#define s second
#define SZ(a) (int)a.size()
typedef long long ll;
using namespace std; 

// We go diagonally as far as we need then left or right

void solve(){
	ll r1, c1, r2, c2;
	cin >> r1 >> c1 >> r2 >> c2;
	if(r1 == r2 && c1 == c2)
	
}

int main()
{
	ios::sync_with_stdio(false); cin.tie(0);
	solve();
}

